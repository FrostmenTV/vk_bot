[VK]
; Токен группы VK с правами на сообщения
token = 

; ID группы (с минусом)
group_id = 

[Database]
; Хост базы данных (обычно localhost для локального сервера)
host = localhost

; Пользователь БД (должен быть создан заранее)
user = 

; Пароль пользователя БД
password = 

; Имя базы данных (должна быть создана заранее)
database = test_bot

; Размер пула соединений (рекомендуется 5-10)
pool_size = 5

[Bot]
; Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
log_level = INFO

; ID администраторов через запятую
admin_ids = 
